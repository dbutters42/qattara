{
  "name": "qattara",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview --host",
    "test": "vitest run",
    "test:gpu": "vite-node tools/gpu-check/hydraulic.ts && vite-node tools/gpu-check/slump.ts"
  },
  "dependencies": {
    "eruda": "^3.4.1"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.66",
    "typescript": "^5.9.3",
    "vite": "^7.1.9",
    "vitest": "^3.2.4",
    "webgpu": "^0.6.1"
  }
}
